# ORB Garment Studio 46

Extract over the existing project and replace matching files, then publish and refresh.

Enabling Glow in the dark or UV reactive no longer opens the color picker automatically. Manual color controls and existing effect behavior remain unchanged. All previous updates are included.

Verified the checkbox handler no longer calls the picker; JavaScript syntax check passed.
