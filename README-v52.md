# ORB Garment Studio 52

Replace matching files, publish and refresh.

The info panel now has a dedicated expandable Keyboard shortcuts section. Individual mappings cover camera top-row keys, lighting numpad keys, editing, rotation, layer reordering, renaming, Escape and larger slider steps. Focus requirements are stated. The former compressed shortcut paragraph was removed. Shortcut behavior is unchanged. All prior updates included.
