# ORB Garment Studio 53

Replace matching files, publish and refresh.

The default light-mode backdrop now matches the sidebar warm gray (#D2D0CA). Switching to light mode or resetting Background Color uses this value. Old light defaults (#F4F4F4 and #DBD9D3) restored from saved designs/browser recovery are updated in light mode, including the background held while Black light is active. Other custom backgrounds retain their values. Explicit PNG export choices such as White remain unchanged.

Syntax checked; live browser verification was unavailable. Includes all earlier updates.
