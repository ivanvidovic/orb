ORB Studio 66

Overlay files, publish, refresh. Pattern Size minimum is now 0.001, with decimal input. Try 0.5, 0.1, 0.01. Maximum remains 100. Subpixel patterns blend to average coverage to avoid false large dots or moire. This extends mathematical spacing by up to 1000x; visible detail remains limited by source/export pixels and preview resolution.

Preview/export pattern consistency and subpixel checks passed; live browser visual QA pending.
