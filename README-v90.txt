ORB Garment Studio v90

Merge into the existing repository, replacing matching files, then hard-refresh. Cumulative v82-v90 update for the v81+ working base.

Export dialog: narrower width; content-sized camera and close-up choices balanced across rows without wrapping labels. Shape, image size and background use separate labeled segmented rows. Canvas width/height and proportional artwork size are grouped separately. Fit to canvas matches input height and aligns beside artwork width. Invalid enabled PSD layouts show a warning and a collapsed-section/footer notice that clears after correction.

Garment framing, artwork treatment and PSD rendering are unchanged from v89.

Verified desktop/mobile layout, selection summaries, fit/warning updates, PNG packaging and cancellation.
